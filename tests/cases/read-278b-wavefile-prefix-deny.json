{
  "script": "pre-read.sh",
  "ac": "AC-278",
  "note": "the .wave/ read exemption is a DIRECTORY prefix, not a string prefix: .wavefile.md still denies W-READ. pre-edit.sh has had this control since edit-265-wavefile-prefix-deny.json; pre-read.sh's copy of the same case-glob had none, so `.wave*` in place of `.wave|.wave/*` would have exempted every path starting with those five bytes and gone unnoticed (progress ledger 140).",
  "seed": { "state": "state/valid-full.json", "files": { ".wavefile.md": "notes\n" } },
  "stdin": { "hook_event_name": "PreToolUse", "tool_name": "Read", "tool_input": { "file_path": ".wavefile.md" } },
  "expect": {
    "decision": "deny",
    "rule": "W-READ",
    "reason_template": "W-READ",
    "reason_contains": [".wavefile.md"]
  }
}
