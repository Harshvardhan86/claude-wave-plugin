{
  "script": "pre-bash.sh",
  "ac": "fix-round-1",
  "note": "controller ruling (fix round 1): a wrapper with its own flag (sudo -E) must not hide the runner, including through npx",
  "seed": { "state": "state/valid-full.json" },
  "stdin": { "hook_event_name": "PreToolUse", "tool_name": "Bash", "tool_input": { "command": "sudo -E npx jest" } },
  "expect": { "decision": "deny", "rule": "W-BASH", "reason_contains": ["jest"] }
}