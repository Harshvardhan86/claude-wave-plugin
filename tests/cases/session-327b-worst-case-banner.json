{
  "script": "session-start.sh",
  "ac": "AC-327 (the worst-case banner)",
  "note": "BOTH advisory clauses at once — compaction just ran with NO checkpoint on disk (the longer of the two compaction texts) and the wave is stale — against a wave id and a last-done phase at their longest. This is the case the reason corpus needs in order to see W-SESSION's maximum length: the banner is assembled from a template plus an `extra` built at runtime, so the only fixture that bounds it is one that turns every clause on. Without it the corpus measured a 344-character banner while the shipped worst case was 440, and the difference arrived not from a commit but from the wall clock passing 24h after the fixture's `started` (fix round 1, item 1).",
  "seed": { "state": "state/full-red-done.json" },
  "env": { "WV_NOW": "2026-09-30T12:00:00Z" },
  "stdin": {
    "hook_event_name": "SessionStart",
    "source": "compact"
  },
  "expect": {
    "decision": "warn",
    "rule": "W-SESSION",
    "reason_contains": [
      "compaction ran",
      "no checkpoint",
      "fresh terminal",
      "wave-close.sh"
    ]
  }
}
