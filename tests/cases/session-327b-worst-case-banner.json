{
  "script": "session-start.sh",
  "ac": "AC-327 (both clauses fire, no checkpoint on disk)",
  "note": "BOTH advisory clauses fire at once — compaction just ran with NO checkpoint on disk, so the LONGER fallback wording is used, and the wave is stale. On an ordinary wave (id `1`, enforce=block) that renders 338 of the 400 the reason corpus allows, so BOTH clauses must appear and nothing may be dropped. This case previously asserted the opposite — '(+1 more)' present, the compaction clause absent — because the extra-clause budget was a worst-case CONSTANT (WV_SESSION_EXTRA_MAX=109) applied unconditionally, and this banner had 209 characters free. That is a superseded contract, not a regression: the property this case exists to pin is the clause ORDER, and it still pins it, by a needle that SPANS the join ('closes it. compaction ran; no checkpoint') — which a swap cannot satisfy while a bare per-clause reason_contains could. session-327d is the collision where the cap does still bind (the maximum accepted input), and it is where the dropped-clause assertions now live; session-327e is the ordinary wave with a checkpoint present.",
  "seed": {
    "state": "state/full-red-done.json"
  },
  "env": { "WV_NOW": "2026-09-30T12:00:00Z" },
  "stdin": {
    "hook_event_name": "SessionStart",
    "source": "compact"
  },
  "expect": {
    "decision": "warn",
    "rule": "W-SESSION",
    "reason_contains": [
      "wave open >24h; scripts/wave-close.sh closes it.",
      "closes it. compaction ran; no checkpoint",
      "compaction ran; no checkpoint — re-derive from .wave/state.json in a fresh terminal (Invariant 7)."
    ],
    "stdout_absent": [
      "(+1 more)",
      ".wave/checkpoints/"
    ]
  }
}
