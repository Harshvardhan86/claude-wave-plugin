# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*~
*.bak

# Node (in case dev tooling is added later)
node_modules/

# Wave runtime artifacts (if anyone runs a wave inside the plugin repo)
.wave/

# Exception: the planning-paths positive fixture under tests/fixtures/ must
# stay tracked so tests/cases/data-planning-paths.sh has a real fixture on a
# fresh clone (hooks/planning-paths.tsv row 1's pattern is literally
# `.wave/**`, so the fixture must keep this exact leaf name to remain a true
# positive for that glob).
!tests/fixtures/**/.wave/
!tests/fixtures/**/.wave/**

# Logs
*.log

# Claude Code session state
.claude-session/

# Build artifacts
dist/
build/

# Local orchestration scratch (never committed)
.superpowers/
